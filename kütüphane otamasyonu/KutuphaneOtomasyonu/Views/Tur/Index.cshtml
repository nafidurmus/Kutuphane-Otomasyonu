@model IEnumerable<KutuphaneOtomasyonu.Models.Tur>

@{
    ViewBag.Title = "Türler";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body style="margin-top: 30px;">
    <p>
        @Html.ActionLink("Yeni Tür Ekle", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.turAd)*@
                Tür Adı
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.turAd)
            </td>
            <td>
                 
                  @Html.ActionLink("Sil", "Delete", "Tur", new { id = item.turID }, null)
                
               
                
            </td>
        </tr>
    }
    </table>
</body>
</html>
